{
  "name": "cncb-azure",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "sls package -r 'East US' -s test",
    "dp:lcl": "sls deploy -v -r 'East US'",
    "rm:lcl": "sls remove -v -r 'East US'"
  },
  "devDependencies": {
    "serverless": "^1.30.1",
    "serverless-azure-functions": "^0.6.0"
  }
}
